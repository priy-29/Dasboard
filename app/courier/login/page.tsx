"use client";
import { FormEvent,useState } from "react";
import { useRouter } from "next/navigation";
import { supabase } from "../../../lib/supabase";
export default function CourierLogin(){const router=useRouter();const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const[loading,setLoading]=useState(false);async function login(e:FormEvent){e.preventDefault();setLoading(true);setError("");const{data,error}=await supabase.auth.signInWithPassword({email,password});if(error){setError(error.message);setLoading(false);return}const{data:row}=await supabase.from("couriers").select("is_active").eq("id",data.user.id).maybeSingle();if(!row?.is_active){await supabase.auth.signOut();setError("Akun ini bukan kurir aktif.");setLoading(false);return}router.replace("/courier");router.refresh()}return <main className="flex min-h-screen items-center justify-center bg-zinc-950 p-5 text-white"><div className="w-full max-w-md rounded-3xl border border-zinc-800 bg-zinc-900 p-7 shadow-2xl"><div className="text-center"><div className="mx-auto flex h-16 w-16 items-center justify-center rounded-2xl bg-orange-500/10 text-4xl">🛵</div><p className="mt-5 text-sm font-bold text-orange-400">DASBOARD KURIR</p><h1 className="mt-1 text-2xl font-black">Login Kurir</h1><p className="mt-1 text-sm text-zinc-500">Gunakan akun yang dibuat oleh admin.</p></div><form onSubmit={login} className="mt-7 space-y-4"><label className="block text-sm">Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} className="mt-2 w-full rounded-xl border border-zinc-800 bg-zinc-950 p-3 outline-none focus:border-orange-500" /></label><label className="block text-sm">Password<input required type="password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-2 w-full rounded-xl border border-zinc-800 bg-zinc-950 p-3 outline-none focus:border-orange-500" /></label>{error&&<p className="rounded-xl border border-red-900 bg-red-950/40 p-3 text-sm text-red-300">⚠️ {error}</p>}<button disabled={loading} className="w-full rounded-xl bg-orange-500 py-3 font-bold text-zinc-950 disabled:opacity-50">{loading?"Masuk...":"Masuk sebagai Kurir"}</button></form></div></main>}
