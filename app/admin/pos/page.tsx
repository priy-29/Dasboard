"use client";

import { useEffect, useMemo, useState } from "react";
import { Banknote, Bell, Check, ChevronRight, Clock3, LogOut, Minus, Package, Plus, Printer, Receipt, Search, ShoppingBag, Trash2, X } from "lucide-react";
import { supabase } from "../../../lib/supabase";

type Product={id:string;name:string;category:string;price:number;stock:number;is_available:boolean};
type CartItem=Product&{quantity:number};
type ReceiptData={id:string;created_at:string;customer_name:string;customer_phone:string;customer_address:string;payment_method:string;delivery_method:string;subtotal:number;delivery_fee:number;total:number;items:CartItem[]};
const ADMIN_EMAIL="maspri2904@gmail.com";
const money=(n:number)=>new Intl.NumberFormat("id-ID",{style:"currency",currency:"IDR",maximumFractionDigits:0}).format(n);
const shortId=(id:string)=>id.slice(0,8).toUpperCase();

export default function AdminPOS(){
 const[products,setProducts]=useState<Product[]>([]),[cart,setCart]=useState<CartItem[]>([]),[query,setQuery]=useState(""),[category,setCategory]=useState("Semua"),[loading,setLoading]=useState(true),[busy,setBusy]=useState(false),[error,setError]=useState(""),[customer,setCustomer]=useState("Pelanggan umum"),[phone,setPhone]=useState(""),[payment,setPayment]=useState("cod"),[receipt,setReceipt]=useState<ReceiptData|null>(null),[mobileCart,setMobileCart]=useState(false),[email,setEmail]=useState("");
 useEffect(()=>{(async()=>{const{data:u}=await supabase.auth.getUser();if(!u.user||u.user.email?.toLowerCase()!==ADMIN_EMAIL){location.href="/admin/login";return}setEmail(u.user.email||"");const{data,error}=await supabase.from("products").select("id,name,category,price,stock,is_available").eq("is_available",true).gt("stock",0).order("category").order("name");if(error)setError(error.message);else setProducts((data||[]) as Product[]);setLoading(false)})();},[]);
 const categories=useMemo(()=>["Semua",...Array.from(new Set(products.map(p=>p.category).filter(Boolean)))],[products]);
 const shown=useMemo(()=>products.filter(p=>(category==="Semua"||p.category===category)&&(!query.trim()||p.name.toLowerCase().includes(query.toLowerCase()))),[products,category,query]);
 const subtotal=useMemo(()=>cart.reduce((s,p)=>s+p.price*p.quantity,0),[cart]);
 function add(p:Product){setCart(c=>{const old=c.find(x=>x.id===p.id);if(old)return c.map(x=>x.id===p.id?{...x,quantity:Math.min(x.quantity+1,p.stock)}:x);return[...c,{...p,quantity:1}]})}
 function change(id:string,d:number){setCart(c=>c.map(x=>x.id===id?{...x,quantity:Math.max(0,Math.min(x.stock,x.quantity+d))}:x).filter(x=>x.quantity>0))}
 async function checkout(){if(!cart.length)return;setBusy(true);setError("");const{data,error:e}=await supabase.rpc("create_order_with_items_v2",{p_customer_name:customer.trim()||"Pelanggan umum",p_customer_phone:phone.trim()||"0000000000",p_customer_address:"Ambil sendiri",p_notes:"Transaksi kasir POS",p_payment_method:payment,p_items:cart.map(x=>({product_id:x.id,quantity:x.quantity})),p_delivery_method:"pickup",p_customer_lat:null,p_customer_lng:null});if(e){setError(e.message);setBusy(false);return}const id=(data as {id:string}).id;const now=new Date().toISOString();setReceipt({id,created_at:now,customer_name:customer.trim()||"Pelanggan umum",customer_phone:phone.trim(),customer_address:"Ambil sendiri",payment_method:payment,delivery_method:"pickup",subtotal,total:Number((data as {total:number}).total),delivery_fee:0,items:[...cart]});setCart([]);setCustomer("Pelanggan umum");setPhone("");setBusy(false);setMobileCart(false);const{data:latest}=await supabase.from("products").select("id,name,category,price,stock,is_available").eq("is_available",true).gt("stock",0).order("category").order("name");setProducts((latest||[]) as Product[])}
 async function logout(){await supabase.auth.signOut();location.href="/admin/login"}
 if(loading)return <main className="min-h-screen bg-[#07090d] text-white grid place-items-center"><div className="text-center"><div className="mx-auto mb-3 h-9 w-9 animate-spin rounded-full border-2 border-white/10 border-t-emerald-400"/><p className="text-sm text-zinc-500">Menyiapkan kasir...</p></div></main>;
 return <main className="pos-shell min-h-screen bg-[#07090d] text-zinc-100 pb-24 lg:pb-0"><header className="sticky top-0 z-40 border-b border-white/[.07] bg-[#080b10]/95 px-4 py-3 backdrop-blur-xl"><div className="mx-auto flex max-w-[1500px] items-center justify-between gap-3"><div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-2xl bg-emerald-400 text-[#06110c]"><ShoppingBag size={20}/></div><div><p className="text-[10px] font-black uppercase tracking-[.22em] text-emerald-300">Kasir POS</p><h1 className="text-base font-black sm:text-lg">Rumah Makan</h1></div></div><div className="flex items-center gap-2"><span className="hidden rounded-full bg-emerald-400/10 px-3 py-2 text-xs font-bold text-emerald-300 sm:block">● Kasir aktif</span><button onClick={()=>location.href="/admin/products"} className="hidden rounded-xl border border-white/10 px-3 py-2 text-xs font-bold sm:block">Produk</button><button onClick={()=>void logout()} className="rounded-xl border border-white/10 p-2 text-zinc-400" title="Keluar"><LogOut size={17}/></button></div></div></header>
 <div className="mx-auto grid max-w-[1500px] gap-4 p-3 md:p-5 lg:grid-cols-[minmax(0,1fr)_390px]">
  <section className="min-w-0"><div className="mb-3 flex items-center justify-between gap-3"><div><p className="text-xs text-zinc-500">Penjualan baru</p><h2 className="text-2xl font-black tracking-tight">Pilih menu</h2></div><button onClick={()=>setMobileCart(true)} className="relative rounded-2xl bg-emerald-400 px-4 py-3 text-xs font-black text-[#06110c] lg:hidden"><Receipt size={16} className="mr-1 inline"/>Keranjang {cart.length>0&&<b className="ml-1">({cart.reduce((s,x)=>s+x.quantity,0)})</b>}</button></div>
   <div className="mb-3 flex gap-2"><div className="relative min-w-0 flex-1"><Search size={17} className="absolute left-3 top-1/2 -translate-y-1/2 text-zinc-600"/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Cari menu..." className="h-12 w-full rounded-2xl border border-white/10 bg-[#11151b] pl-10 pr-4 text-sm outline-none focus:border-emerald-400/50"/></div><button onClick={()=>setCategory("Semua")} className="hidden rounded-2xl border border-white/10 px-4 text-xs font-bold sm:block">Semua</button></div>
   <div className="mb-4 flex gap-2 overflow-x-auto pb-1">{categories.map(c=><button key={c} onClick={()=>setCategory(c)} className={`shrink-0 rounded-full px-4 py-2 text-xs font-bold ${category===c?"bg-emerald-400 text-[#06110c]":"border border-white/10 bg-white/[.03] text-zinc-500"}`}>{c}</button>)}</div>
   {error&&<div className="mb-4 rounded-2xl border border-red-500/20 bg-red-500/10 p-3 text-sm text-red-300">⚠️ {error}</div>}
   <div className="grid grid-cols-2 gap-3 sm:grid-cols-3 xl:grid-cols-4">{shown.map(p=><button key={p.id} onClick={()=>add(p)} className="group min-h-[145px] rounded-[24px] border border-white/[.08] bg-[#101419] p-4 text-left transition hover:-translate-y-0.5 hover:border-emerald-400/30 hover:bg-[#141a20] active:scale-[.98]"><div className="flex h-11 w-11 items-center justify-center rounded-2xl bg-emerald-400/10 text-emerald-300"><Package size={20}/></div><p className="mt-4 line-clamp-2 text-sm font-black leading-5">{p.name}</p><p className="mt-1 text-[10px] text-zinc-600">Stok {p.stock}</p><p className="mt-2 text-sm font-black text-emerald-300">{money(p.price)}</p></button>)}{shown.length===0&&<div className="col-span-full rounded-3xl border border-dashed border-white/10 p-12 text-center text-sm text-zinc-600">Menu tidak ditemukan.</div>}</div>
  </section>
  <CartPanel cart={cart} subtotal={subtotal} customer={customer} setCustomer={setCustomer} phone={phone} setPhone={setPhone} payment={payment} setPayment={setPayment} change={change} remove={id=>setCart(c=>c.filter(x=>x.id!==id))} checkout={checkout} busy={busy} mobile={false}/>
 </div>
 {mobileCart&&<div className="fixed inset-0 z-[100] bg-black/70 backdrop-blur-sm lg:hidden"><div className="absolute bottom-0 left-0 right-0 max-h-[92vh] overflow-y-auto rounded-t-[30px] border border-white/10 bg-[#0d1117] p-4"><div className="mb-2 flex items-center justify-between"><b>Keranjang</b><button onClick={()=>setMobileCart(false)} className="rounded-xl border border-white/10 p-2"><X size={17}/></button></div><CartPanel cart={cart} subtotal={subtotal} customer={customer} setCustomer={setCustomer} phone={phone} setPhone={setPhone} payment={payment} setPayment={setPayment} change={change} remove={id=>setCart(c=>c.filter(x=>x.id!==id))} checkout={checkout} busy={busy} mobile/></div></div>}
 {receipt&&<ReceiptModal data={receipt} onClose={()=>setReceipt(null)}/>}<style>{`@media print{body *{visibility:hidden!important}.pos-receipt-print,.pos-receipt-print *{visibility:visible!important}.pos-receipt-print{position:absolute!important;left:0!important;top:0!important;width:80mm!important;margin:0!important;padding:4mm!important;background:white!important;color:#000!important;font-family:monospace!important} .pos-receipt-print *{color:#000!important} @page{size:80mm auto;margin:0}}`}</style>
 </main>;
}

function CartPanel({cart,subtotal,customer,setCustomer,phone,setPhone,payment,setPayment,change,remove,checkout,busy,mobile}:{cart:CartItem[];subtotal:number;customer:string;setCustomer:(v:string)=>void;phone:string;setPhone:(v:string)=>void;payment:string;setPayment:(v:string)=>void;change:(id:string,d:number)=>void;remove:(id:string)=>void;checkout:()=>void;busy:boolean;mobile:boolean}){return <aside className={`${mobile?"":"hidden lg:block"} lg:sticky lg:top-[78px] lg:h-[calc(100vh-96px)]`}><div className="flex h-full flex-col rounded-[28px] border border-white/[.08] bg-[#101419] p-4 shadow-2xl shadow-black/20"><div className="flex items-center justify-between"><div><p className="text-[10px] font-black uppercase tracking-[.2em] text-emerald-300">Transaksi</p><h2 className="mt-1 text-xl font-black">Keranjang</h2></div><span className="rounded-full bg-white/[.05] px-3 py-1.5 text-[10px] font-bold text-zinc-500">{cart.reduce((s,x)=>s+x.quantity,0)} item</span></div><div className="mt-4 flex-1 overflow-y-auto pr-1">{cart.length===0?<div className="grid min-h-[180px] place-items-center rounded-2xl border border-dashed border-white/10 text-center text-zinc-600"><div><Receipt className="mx-auto mb-2"/><p className="text-sm font-bold">Keranjang kosong</p><p className="mt-1 text-xs">Tap menu untuk menambahkan.</p></div></div>:<div className="space-y-2">{cart.map(x=><div key={x.id} className="rounded-2xl bg-black/20 p-3"><div className="flex justify-between gap-2"><div className="min-w-0"><b className="block truncate text-sm">{x.name}</b><span className="text-xs text-zinc-600">{money(x.price)}</span></div><b className="text-sm">{money(x.price*x.quantity)}</b></div><div className="mt-3 flex items-center justify-between"><div className="flex items-center gap-1 rounded-xl border border-white/10 p-1"><button onClick={()=>change(x.id,-1)} className="grid h-8 w-8 place-items-center rounded-lg hover:bg-white/10"><Minus size={14}/></button><b className="w-7 text-center text-xs">{x.quantity}</b><button onClick={()=>change(x.id,1)} className="grid h-8 w-8 place-items-center rounded-lg hover:bg-white/10"><Plus size={14}/></button></div><button onClick={()=>remove(x.id)} className="rounded-lg p-2 text-zinc-600 hover:text-red-300"><Trash2 size={15}/></button></div></div>)}</div>}</div><div className="mt-4 border-t border-white/10 pt-4"><div className="grid grid-cols-2 gap-2"><input value={customer} onChange={e=>setCustomer(e.target.value)} placeholder="Nama pelanggan" className="h-11 rounded-xl border border-white/10 bg-black/20 px-3 text-sm outline-none focus:border-emerald-400/50"/><input value={phone} onChange={e=>setPhone(e.target.value)} placeholder="No. HP (opsional)" className="h-11 rounded-xl border border-white/10 bg-black/20 px-3 text-sm outline-none focus:border-emerald-400/50"/></div><div className="mt-2 grid grid-cols-3 gap-2">{[["cod","Tunai"],["qris","QRIS"],["transfer","Transfer"]].map(([v,l])=><button key={v} onClick={()=>setPayment(v)} className={`rounded-xl py-3 text-[11px] font-black ${payment===v?"bg-emerald-400 text-[#06110c]":"border border-white/10 text-zinc-500"}`}>{l}</button>)}</div><div className="mt-4 flex items-end justify-between"><div><p className="text-xs text-zinc-600">Total</p><p className="text-2xl font-black text-emerald-300">{money(subtotal)}</p></div><button disabled={!cart.length||busy} onClick={()=>void checkout()} className="rounded-2xl bg-emerald-400 px-5 py-3 font-black text-[#06110c] disabled:opacity-40">{busy?"Memproses...":"Bayar & Buat Struk"}</button></div></div></div></aside>}

function ReceiptModal({data,onClose}:{data:ReceiptData;onClose:()=>void}){return <div className="fixed inset-0 z-[120] grid place-items-center bg-black/80 p-4 backdrop-blur-sm"><div className="w-full max-w-md rounded-[28px] border border-white/10 bg-[#101419] p-4 shadow-2xl"><div className="mb-3 flex items-center justify-between"><div><p className="text-[10px] font-black uppercase tracking-[.2em] text-emerald-300">Pembayaran berhasil</p><h2 className="text-xl font-black">Struk siap dicetak</h2></div><button onClick={onClose} className="rounded-xl border border-white/10 p-2"><X size={17}/></button></div><div className="pos-receipt-print rounded-xl bg-white p-4 text-black"><div className="text-center"><b className="text-lg">RUMAH MAKAN</b><p className="text-xs">Struk Pembayaran</p></div><div className="my-3 border-t border-dashed border-black"/><div className="text-xs"><div className="flex justify-between"><span>No</span><b>#{shortId(data.id)}</b></div><div className="mt-1 flex justify-between"><span>Waktu</span><span>{new Date(data.created_at).toLocaleString("id-ID")}</span></div><div className="mt-1 flex justify-between"><span>Pelanggan</span><span>{data.customer_name}</span></div></div><div className="my-3 border-t border-dashed border-black"/>{data.items.map(x=><div key={x.id} className="mb-2 text-xs"><div className="flex justify-between gap-2"><span>{x.name} × {x.quantity}</span><b>{money(x.price*x.quantity)}</b></div></div>)}<div className="my-3 border-t border-dashed border-black"/><div className="flex justify-between text-xs"><span>Subtotal</span><b>{money(data.subtotal)}</b></div><div className="mt-1 flex justify-between text-base font-black"><span>TOTAL</span><b>{money(data.total)}</b></div><div className="mt-2 text-xs">Pembayaran: {data.payment_method==="cod"?"Tunai":data.payment_method.toUpperCase()}</div><div className="mt-5 text-center text-[10px]">Terima kasih 🙏</div></div><div className="mt-3 grid grid-cols-2 gap-2"><button onClick={()=>window.print()} className="rounded-2xl bg-emerald-400 py-3 font-black text-[#06110c]"><Printer size={16} className="mr-1 inline"/>Cetak Struk</button><button onClick={onClose} className="rounded-2xl border border-white/10 py-3 font-bold">Selesai</button></div></div></div>}
