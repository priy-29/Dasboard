"use client";
import { useCallback, useEffect, useMemo, useState } from "react";
import LiveMap from "../../../components/LiveMap";
import { supabase } from "../../../lib/supabase";
type OrderStatus={id:string;status:string;delivery_method:"delivery"|"pickup";customer_lat:number|null;customer_lng:number|null;delivery_distance_km:number|null;delivery_fee:number;total:number;created_at:string;courier_lat:number|null;courier_lng:number|null;courier_updated_at:string|null;courier_tracking:boolean};
const RESTAURANT_LAT=-7.396963,RESTAURANT_LNG=109.199585;
const steps=["pending","confirmed","preparing","delivering","completed"];
const labels:Record<string,string>={pending:"Pesanan diterima",confirmed:"Pesanan dikonfirmasi",preparing:"Sedang disiapkan",delivering:"Sedang diantar",completed:"Pesanan selesai",cancelled:"Pesanan dibatalkan"};
const rupiah=(n:number)=>new Intl.NumberFormat("id-ID",{style:"currency",currency:"IDR",maximumFractionDigits:0}).format(n);
export default function StatusPage(){const[id,setId]=useState("");const[order,setOrder]=useState<OrderStatus|null>(null);const[loading,setLoading]=useState(false);const[error,setError]=useState("");const check=useCallback(async(orderId:string)=>{if(!orderId)return;const{data,error:e}=await supabase.rpc("get_order_status",{p_order_id:orderId});if(e){setError(e.message);return}if(!data){setError("Pesanan tidak ditemukan. Periksa nomor pesanan.");return}setError("");setOrder(data as OrderStatus)},[]);
useEffect(()=>{const saved=sessionStorage.getItem("dasboard_order_id")||"";if(saved){setId(saved);check(saved)}},[check]);useEffect(()=>{if(!order?.id||order.status==="completed"||order.status==="cancelled")return;const t=window.setInterval(()=>check(order.id),3000);return()=>window.clearInterval(t)},[order?.id,order?.status,check]);async function submit(e:React.FormEvent){e.preventDefault();const v=id.trim();if(!v)return;setLoading(true);setError("");sessionStorage.setItem("dasboard_order_id",v);await check(v);setLoading(false)}
const cancelled=order?.status==="cancelled",current=order?steps.indexOf(order.status):-1;const mapUrl=useMemo(()=>order?.customer_lat!=null&&order?.customer_lng!=null?`https://www.google.com/maps/dir/?api=1&origin=${RESTAURANT_LAT},${RESTAURANT_LNG}&destination=${order.customer_lat},${order.customer_lng}&travelmode=driving`:"",[order]);
const points=order?[{lat:RESTAURANT_LAT,lng:RESTAURANT_LNG,label:"Rumah makan",emoji:"🏠",className:"bg-emerald-500"},...(order.customer_lat!=null&&order.customer_lng!=null?[{lat:order.customer_lat,lng:order.customer_lng,label:"Pelanggan",emoji:"📍",className:"bg-blue-500"}]:[]),...(order.courier_lat!=null&&order.courier_lng!=null?[{lat:order.courier_lat,lng:order.courier_lng,label:order.courier_tracking?"Kurir · LIVE":"Kurir",emoji:"🛵",className:"bg-orange-500"}]:[])]:[];
return <main className="min-h-screen bg-zinc-950 p-5 text-white"><div className="mx-auto max-w-md"><button onClick={()=>location.href="/menu"} className="mb-6 text-sm text-zinc-500">← Kembali ke Menu</button><div className="text-center"><div className="text-5xl">📦</div><h1 className="mt-3 text-2xl font-bold">Status Pesanan</h1><p className="mt-1 text-sm text-zinc-500">Posisi kurir diperbarui otomatis.</p></div><form onSubmit={submit} className="mt-7 flex gap-2"><input value={id} onChange={e=>setId(e.target.value)} className="min-w-0 flex-1 rounded-xl border border-zinc-800 bg-zinc-900 p-3 text-sm outline-none focus:border-emerald-500" placeholder="Masukkan nomor pesanan"/><button disabled={loading} className="rounded-xl bg-emerald-500 px-4 py-3 text-sm font-semibold text-zinc-950">{loading?"Cek...":"Cek"}</button></form>{error&&<p className="mt-4 rounded-xl border border-red-900 bg-red-950/40 p-3 text-sm text-red-400">{error}</p>}
{order&&<section className="mt-5 rounded-3xl border border-zinc-800 bg-zinc-900 p-5"><div className="flex items-center justify-between gap-3"><div><p className="text-xs text-zinc-500">Nomor Pesanan</p><p className="mt-1 break-all font-mono text-sm">{order.id}</p></div><span className="rounded-full bg-zinc-800 px-3 py-1 text-xs">{order.delivery_method==="delivery"?"🚚 Diantar":"🏠 Ambil sendiri"}</span></div><div className="mt-7 text-center"><div className="text-4xl">{cancelled?"❌":order.status==="completed"?"✅":order.courier_tracking?"🛵":"👨‍🍳"}</div><h2 className="mt-2 text-xl font-bold">{order.courier_tracking?"Kurir sedang OTW":labels[order.status]||order.status}</h2></div>{!cancelled&&<div className="mt-7 space-y-4">{steps.map((step,i)=><div key={step} className="flex items-center gap-3"><div className={`flex h-9 w-9 shrink-0 items-center justify-center rounded-full text-sm font-bold ${i<=current?"bg-emerald-500 text-zinc-950":"bg-zinc-800 text-zinc-500"}`}>{i<=current?"✓":i+1}</div><p className={`font-medium ${i<=current?"text-white":"text-zinc-500"}`}>{labels[step]}</p></div>)}</div>}
{order.delivery_method==="delivery"&&!cancelled&&order.customer_lat!=null&&order.customer_lng!=null&&<div className="mt-6 overflow-hidden rounded-2xl border border-zinc-800 bg-zinc-950"><div className="p-4"><div className="flex items-center justify-between"><div><p className="font-semibold">🗺️ Live Tracking</p><p className="mt-1 text-xs text-zinc-500">Rumah makan → Kurir → Pelanggan</p></div>{order.courier_tracking&&<span className="rounded-full bg-red-500 px-2 py-1 text-[10px] font-bold text-white">● LIVE</span>}</div></div><LiveMap points={points}/>{order.courier_tracking&&<div className="border-t border-zinc-800 bg-emerald-950/20 p-3 text-center text-xs text-emerald-300">🛵 Kurir sedang menuju lokasi kamu</div>}<div className="grid grid-cols-2 gap-2 p-4 text-xs"><div className="rounded-xl bg-zinc-900 p-3"><p className="text-zinc-500">Jarak pengantaran</p><p className="mt-1 font-semibold">{order.delivery_distance_km!=null?`${Number(order.delivery_distance_km).toFixed(1)} km`:"-"}</p></div><div className="rounded-xl bg-zinc-900 p-3"><p className="text-zinc-500">Ongkir</p><p className="mt-1 font-semibold">{rupiah(order.delivery_fee||0)}</p></div></div><a href={mapUrl} target="_blank" rel="noreferrer" className="mx-4 mb-4 block rounded-xl bg-emerald-500 py-3 text-center text-sm font-bold text-zinc-950">📍 Buka Google Maps & Rute</a></div>}
{order.delivery_method==="pickup"&&!cancelled&&<div className="mt-6 rounded-2xl border border-zinc-800 bg-zinc-950 p-4 text-center text-sm text-zinc-400">🏠 Pesanan diambil sendiri. Tidak ada ongkir dan GPS.</div>}{cancelled&&<div className="mt-6 rounded-xl bg-red-950/40 p-3 text-center text-sm text-red-400">Pesanan ini dibatalkan oleh admin.</div>}<div className="mt-6 border-t border-zinc-800 pt-4 flex justify-between text-sm"><span className="text-zinc-500">Total</span><span className="font-bold">{rupiah(order.total||0)}</span></div></section>}<div className="mt-5 text-center"><button onClick={()=>location.href="/menu"} className="text-sm text-emerald-400">Pesan lagi</button></div></div></main>}
