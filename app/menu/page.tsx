"use client";
import { useEffect,useMemo,useState } from "react";import { createClient } from "@supabase/supabase-js";import { useRouter } from "next/navigation";import { ShoppingCart,ArrowRight,X } from "lucide-react";
type Product={id:string;name:string;category:string;price:number;description:string|null;stock:number;is_available:boolean;image_url:string|null};type CartItem={product_id:string;quantity:number;options:Record<string,string>;special_note:string};
const supabase=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!);const rupiah=(n:number)=>new Intl.NumberFormat("id-ID",{style:"currency",currency:"IDR",maximumFractionDigits:0}).format(n);
const defaultOptions={"Tingkat Kepedasan":"Normal","Suhu Minuman":"Tidak ada"};
export default function MenuPage(){const router=useRouter();const[products,setProducts]=useState<Product[]>([]);const[category,setCategory]=useState("Semua");const[cart,setCart]=useState<CartItem[]>([]);const[showCart,setShowCart]=useState(false);const[selected,setSelected]=useState<Product|null>(null);const[options,setOptions]=useState(defaultOptions);const[note,setNote]=useState("");const[loading,setLoading]=useState(true);const[error,setError]=useState("");
useEffect(()=>{(async()=>{const{data,error}=await supabase.from("products").select("id,name,category,price,description,stock,is_available,image_url").order("created_at");if(error)setError(error.message);else setProducts((data||[]) as Product[]);try{const old=JSON.parse(sessionStorage.getItem("dasboard_cart_items")||"[]");if(Array.isArray(old))setCart(old)}catch{}setLoading(false)})()},[]);
const categories=["Semua",...Array.from(new Set(products.map(p=>p.category)))];const visible=products.filter(p=>category==="Semua"||p.category===category);const count=cart.reduce((s,i)=>s+i.quantity,0);const total=useMemo(()=>cart.reduce((s,i)=>{const p=products.find(x=>x.id===i.product_id);return s+(p?p.price*i.quantity:0)},0),[cart,products]);
function openAdd(p:Product){if(!p.is_available||p.stock<=0)return;setSelected(p);setOptions({...defaultOptions});setNote("")}
function addSelected(){if(!selected)return;const item:CartItem={product_id:selected.id,quantity:1,options,special_note:note.trim()};setCart(c=>{const n=[...c,item];sessionStorage.setItem("dasboard_cart_items",JSON.stringify(n));return n});setSelected(null)}
function remove(i:number){setCart(c=>{const n=c.map((x,k)=>k===i?{...x,quantity:x.quantity-1}:x).filter(x=>x.quantity>0);sessionStorage.setItem("dasboard_cart_items",JSON.stringify(n));return n})}
function add(i:number){setCart(c=>{const item=c[i];const p=products.find(x=>x.id===item?.product_id);if(!p||item.quantity>=p.stock)return c;const n=c.map((x,k)=>k===i?{...x,quantity:x.quantity+1}:x);sessionStorage.setItem("dasboard_cart_items",JSON.stringify(n));return n})}
function checkout(){if(cart.length){sessionStorage.setItem("dasboard_cart_items",JSON.stringify(cart));router.push("/menu/checkout")}}
return <main className="min-h-screen bg-zinc-950 pb-36 text-white"><header className="border-b border-zinc-800 bg-zinc-900/80"><div className="mx-auto max-w-5xl px-5 py-5"><p className="text-sm text-emerald-400">Selamat datang 👋</p><h1 className="mt-1 text-2xl font-bold">Rumah Makan Kita</h1><p className="mt-1 text-sm text-zinc-500">Pilih menu, atur variasi, lalu masukkan ke keranjang.</p></div></header><div className="mx-auto max-w-5xl px-5"><div className="sticky top-0 z-10 -mx-5 overflow-x-auto bg-zinc-950/95 px-5 py-4 backdrop-blur"><div className="flex gap-2">{categories.map(c=><button key={c} onClick={()=>setCategory(c)} className={`rounded-full px-4 py-2 text-sm ${category===c?"bg-emerald-500 font-semibold text-zinc-950":"bg-zinc-900 text-zinc-400"}`}>{c}</button>)}</div></div>{loading?<p className="py-12 text-center text-zinc-500">Memuat menu...</p>:error?<p className="py-12 text-center text-red-400">Gagal memuat menu: {error}</p>:<section className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{visible.map(p=><article key={p.id} className="overflow-hidden rounded-2xl border border-zinc-800 bg-zinc-900"><div className="relative h-44 bg-zinc-800">{p.image_url?<img src={p.image_url} alt={p.name} className={`h-full w-full object-cover ${!p.is_available?"opacity-40 grayscale":""}`} loading="lazy"/>:<div className="flex h-full items-center justify-center text-6xl">🍽️</div>}{!p.is_available&&<span className="absolute left-3 top-3 rounded-full bg-red-500 px-3 py-1 text-xs font-black text-white">Sold Out</span>}</div><div className="p-4"><p className="text-xs text-emerald-400">{p.category}</p><h2 className="mt-1 font-semibold">{p.name}</h2><p className="mt-1 text-xs leading-5 text-zinc-500">{p.description||"Menu pilihan rumah makan."}</p><div className="mt-4 flex items-center justify-between gap-3"><span className="font-bold">{rupiah(p.price)}</span><button disabled={!p.is_available||p.stock<=0} onClick={()=>openAdd(p)} className="rounded-xl bg-emerald-500 px-4 py-2 text-sm font-semibold text-zinc-950 disabled:cursor-not-allowed disabled:bg-zinc-700 disabled:text-zinc-400">{p.is_available&&p.stock>0?"Tambah ke Keranjang":"Sold Out"}</button></div></div></article>)}</section>}</div>{count>0&&<div className="fixed inset-x-0 bottom-0 z-40 px-4 pb-[max(12px,env(safe-area-inset-bottom))] pt-3"><div className="mx-auto flex max-w-5xl items-center gap-2 rounded-2xl border border-emerald-400/20 bg-zinc-900/95 p-2 shadow-2xl backdrop-blur"><button onClick={()=>setShowCart(true)} className="flex min-w-0 flex-1 items-center gap-3 rounded-xl px-3 py-2.5 text-left"><span className="relative grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-emerald-500 text-zinc-950"><ShoppingCart size={19}/><span className="absolute -right-1 -top-1 grid h-5 min-w-5 place-items-center rounded-full bg-white px-1 text-[10px] font-black">{count}</span></span><span><b className="block text-sm">Keranjang</b><small className="text-xs text-zinc-400">{count} item · {rupiah(total)}</small></span></button><button onClick={checkout} className="flex shrink-0 items-center gap-2 rounded-xl bg-emerald-500 px-4 py-3 text-sm font-black text-zinc-950">Checkout <ArrowRight size={17}/></button></div></div>}{showCart&&<div className="fixed inset-0 z-50 bg-black/75 p-4" onClick={()=>setShowCart(false)}><div className="mx-auto mt-8 max-h-[85vh] max-w-lg overflow-y-auto rounded-3xl border border-zinc-800 bg-zinc-900 p-5" onClick={e=>e.stopPropagation()}><div className="flex justify-between"><div><h2 className="text-xl font-bold">Keranjang</h2><p className="mt-1 text-xs text-zinc-500">Variasi dan catatan ikut tersimpan.</p></div><button onClick={()=>setShowCart(false)}><X/></button></div><div className="mt-5 space-y-3">{cart.map((i,k)=>{const p=products.find(x=>x.id===i.product_id);return p?<div key={k} className="rounded-xl bg-zinc-800 p-3"><div className="flex items-start justify-between gap-3"><div><p className="font-medium">{p.name}</p><p className="text-sm text-zinc-400">{Object.entries(i.options).filter(([,v])=>v&&v!=="Tidak ada").map(([a,b])=>`${a}: ${b}`).join(" · ")}</p>{i.special_note&&<p className="text-xs text-amber-300">Catatan: {i.special_note}</p>}</div><b>{rupiah(p.price*i.quantity)}</b></div><div className="mt-3 flex items-center gap-2"><button onClick={()=>remove(k)} className="rounded-lg bg-zinc-700 px-3 py-1.5">−</button><span>{i.quantity}</span><button onClick={()=>add(k)} className="rounded-lg bg-emerald-500 px-3 py-1.5 text-zinc-950">+</button></div></div>:null})}</div><div className="mt-5 border-t border-zinc-800 pt-4"><div className="flex justify-between text-lg font-bold"><span>Total</span><span>{rupiah(total)}</span></div><button onClick={checkout} className="mt-4 w-full rounded-xl bg-emerald-500 py-3.5 font-black text-zinc-950">Lanjut ke Checkout</button></div></div></div>}{selected&&<div className="fixed inset-0 z-[60] flex items-end justify-center bg-black/75 p-4 sm:items-center" onClick={()=>setSelected(null)}><div className="w-full max-w-lg rounded-3xl border border-zinc-800 bg-zinc-900 p-5" onClick={e=>e.stopPropagation()}><div className="flex justify-between"><div><p className="text-xs text-emerald-400">{selected.category}</p><h2 className="text-xl font-black">{selected.name}</h2><p className="mt-1 font-bold">{rupiah(selected.price)}</p></div><button onClick={()=>setSelected(null)}><X/></button></div><div className="mt-5 space-y-4"><label className="block text-sm font-semibold">Tingkat Kepedasan<select value={options["Tingkat Kepedasan"]} onChange={e=>setOptions(o=>({...o,"Tingkat Kepedasan":e.target.value}))} className="mt-2 w-full rounded-xl border border-zinc-700 bg-zinc-800 p-3"><option>Normal</option><option>Tidak pedas</option><option>Sedang</option><option>Pedas</option><option>Extra pedas</option></select></label><label className="block text-sm font-semibold">Opsi Panas/Es<select value={options["Suhu Minuman"]} onChange={e=>setOptions(o=>({...o,"Suhu Minuman":e.target.value}))} className="mt-2 w-full rounded-xl border border-zinc-700 bg-zinc-800 p-3"><option>Tidak ada</option><option>Panas</option><option>Es</option></select></label><label className="block text-sm font-semibold">Catatan Khusus<textarea value={note} onChange={e=>setNote(e.target.value)} rows={3} className="mt-2 w-full rounded-xl border border-zinc-700 bg-zinc-800 p-3 font-normal" placeholder="Contoh: tanpa bawang, kuah dipisah..."/></label><button onClick={addSelected} className="w-full rounded-xl bg-emerald-500 py-3.5 font-black text-zinc-950">Tambahkan ke Keranjang</button></div></div></div>}</main>}
